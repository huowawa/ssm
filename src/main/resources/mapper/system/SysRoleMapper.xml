<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.soft.platform.system.mapper.SysRoleMapper">
    <resultMap id="BaseResultMap" type="com.soft.platform.system.model.SysRole">
        <id column="role_id" property="roleId" jdbcType="VARCHAR"/>
        <result column="role_name" property="roleName" jdbcType="VARCHAR"/>
        <result column="role_code" property="roleCode" jdbcType="VARCHAR"/>
    </resultMap>

    <select id="findRoleByGroupId" resultMap="BaseResultMap">
        select   r.role_id,r.role_name,r.role_code  from t_msjw_system_sysrole r
        where r.group_id = #{groupid}
    </select>

    <insert id="saveSysRole" parameterType="SysRole">
          insert  into t_msjw_system_sysrole(role_id,role_name,role_code)
          values (#{roleId},#{roleName},#{roleCode})
    </insert>

    <update id="modifySysRole" parameterType="SysRole">
        update t_msjw_system_sysrole
        set role_name=#{roleName},
        role_code=#{roleCode}
        where  role_id=#{roleId}
    </update>

    <delete id="removeSysRole">
        delete from  t_msjw_system_sysrole where role_id=#{roleId}
    </delete>

</mapper>